You are an expert UI vision parser. Extract ALL visible UI elements from this screenshot in ONE pass.

Output ONE line per element using this exact format:
  <label_text> - <ocr_type> - <intent>

Strict rules:
1) Output format:
   - Exactly 3 parts separated by " - ".
   - No numbering, no bullets, no markdown, no blank lines, no extra commentary.

2) label_text:
   - Use the exact on-screen text closest to the element (do NOT paraphrase).
   - For inputs, prefer the visible label (above/beside) or placeholder text.
   - For buttons/links, use the visible clickable text.
   - Include visible navigation items, cards/tiles, tabs, menu items, and key page headings
     that a user would click or rely on to navigate.
   - For tables: ALWAYS list ALL column headers (thead/th) as separate label_text lines,
     even if the header text is generic (e.g., "Action") or appears in multiple tables.
    - For icon-only clickable elements (no visible text), set label_text to a short descriptive
      name like "cart icon", "search icon", "menu icon", "close icon", "settings icon".
    - If an icon button has an adjacent visible text label (e.g., avatar + "John Doe"),
      use the visible text as label_text and still set ocr_type to iconbutton.
    - If you see a user avatar/profile image (face/headshot) that is clickable, label it as "profile icon".
   - Never leave label_text blank.

3) ocr_type (choose exactly ONE):
   - textbox, button, iconbutton, label, checkbox, select, link, image

   Selection guidance:
   - Always use select for dropdowns/picklists/comboboxes or any field with a chevron/down-arrow/caret indicator.
   - If the field shows a default/pre-selected value AND has a dropdown indicator, it is still select.
   - If the field is empty/blank BUT has a dropdown indicator, it is still select.
   - Do not classify a dropdown-looking control as textbox just because the box is blank.
   - Use textbox for free-text entry fields without a dropdown indicator.
   - Date inputs with a calendar icon are textbox unless there is also a dropdown chevron/caret for choosing from a list.

   Critical distinction:
   - A blank rectangular field with a chevron/down-arrow on the right = select
   - A blank rectangular field with no chevron/down-arrow = textbox
   - A field with a calendar icon = textbox

   Generic correction examples:
   - A blank field with a chevron/down-arrow on the right must be select.
   - A blank field with no chevron/down-arrow must be textbox.
   - A field with a calendar icon must be textbox unless it also clearly shows a dropdown affordance.

4) intent:
   - A concise snake_case token derived from label_text and the type, e.g.:
     Email -> email_field
     Password -> password_field
     Login -> login_action
     Account Type -> account_type_select

5) Exhaustiveness:
   - List EVERY visible input, dropdown, checkbox, link, and button on the screen.
   - For tables, list EVERY visible column header and any row-level action labels (e.g., "edit", "delete").
   - Do not stop early; keep going until all elements are listed.

Examples:
  Username - textbox - username_field
  Password - textbox - password_field
  Login - button - login_action
  Remember Me - checkbox - remember_me_checkbox
  Account Type - select - account_type_select
  Originating Lead - textbox - originating_lead_field
  Preferred Method of Contact - select - preferred_method_of_contact_select
  Primary Time Zone - select - primary_time_zone_select
  Lead Source - select - lead_source_select
  Company Name - textbox - company_name_field
  Home Phone - textbox - home_phone_field
  Customer Service - link - customer_service_action

